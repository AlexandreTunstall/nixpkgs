# Versioning

The upstream repository does not have tags for versions.

Judging by the upload dates on Hackage and the commit dates in the Git
repository, it seems that the commits that bump the version number correspond to
the sources for the new version.
Therefore for all but the HEAD version, we always use the first commit for a
particular version number to build that version.
