diff --git a/lib/Data/Char.hs b/lib/Data/Char.hs
index 345f5753..1d08dc39 100644
--- a/lib/Data/Char.hs
+++ b/lib/Data/Char.hs
@@ -67,7 +67,7 @@ isLower c =
   if primCharLE c '\177' then
      isAsciiLower c
   else
-     U.isLower c
+     False
 
 isAsciiLower :: Char -> Bool
 isAsciiLower c = 'a' <= c && c <= 'z'
@@ -77,7 +77,7 @@ isUpper c =
   if isAscii c then
     isAsciiUpper c
   else
-    U.isUpper c
+    False
 
 isAsciiUpper :: Char -> Bool
 isAsciiUpper c = 'A' <= c && c <= 'Z'
@@ -87,7 +87,7 @@ isAlpha c =
   if isAscii c then
     isLower c || isUpper c
   else
-    U.isAlpha c
+    False
 
 isDigit :: Char -> Bool
 isDigit c = '0' <= c && c <= '9'
@@ -103,21 +103,21 @@ isAlphaNum c =
   if isAscii c then
     isAlpha c || isDigit c
   else
-    U.isAlphaNum c
+    False
 
 isSymbol :: Char -> Bool
 isSymbol c =
  if isAscii c then
    c `xelem` "$+<=>^`|~"
  else
-   U.isSymbol c
+   False
 
 isPunctuation :: Char -> Bool
 isPunctuation c =
   if isAscii c then
     c `xelem` "!\"#%&'()*,-./:;?@[\\]_{}"
   else
-    U.isPunctuation c
+    False
 
 -- Don't want to import Data.List
 xelem :: Char -> [Char] -> Bool
@@ -129,14 +129,14 @@ isPrint c =
   if isAscii c then
     ' ' <= c && c <= '~'
   else
-    U.isPrint c
+    False
 
 isSpace :: Char -> Bool
 isSpace c =
   if isAscii c then
     c == ' ' || c == '\t' || c == '\n'
   else
-    U.isSpace c
+    False
 
 isAscii :: Char -> Bool
 isAscii c = c <= '\127'
@@ -157,12 +157,12 @@ intToDigit i | i < 10 = chr (ord '0' + i)
 toLower :: Char -> Char
 toLower c | 'A' <= c && c <= 'Z' = chr (ord c - ord 'A' + ord 'a')
           | isAscii c = c
-          | True = U.toLower c
+          | True = c
 
 toUpper :: Char -> Char
 toUpper c | 'a' <= c && c <= 'a' = chr (ord c - ord 'a' + ord 'A')
           | isAscii c = c
-          | True = U.toUpper c
+          | True = c
 
 instance Show Char where
   showsPrec _ '\'' = showString "'\\''"
